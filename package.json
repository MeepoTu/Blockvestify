{
  "name": "eliza-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "start": "pnpm --filter @elizaos/eliza-starter start",
    "dev:starter": "pnpm --filter @elizaos/eliza-starter start",
    "build:starter": "pnpm --filter @elizaos/eliza-starter build",
    "start:client": "pnpm --dir client dev",
    "clean": "pnpm -r clean"
  },
  "dependencies": {
    "@elizaos/plugin-nasa": "workspace:*",
    "@elizaos/plugin-sui-defi": "workspace:*",
    "@elizaos/plugin-bluefin": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
